require 'spec_helper'
require 'space'

# TODO this doesn't seem to actually catch issues? 
describe Game do
  # it 'should integrate with game engine' do
  #   expect(subject.engine).to receive(:step)
  #   expect(subject.engine).to receive(:render)
  #   subject.update
  #   subject.draw
  # end
end
